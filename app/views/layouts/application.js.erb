$(function () {
<% flash.each do |key, value| %>
  $().toastmessage('showToast', {
  text     : "<%= value %>",
  sticky   : <%= get_toast_sticky(key) %>,
  position : 'top-center',
  type     : "<%= get_toast_type(key) %>"
  });
<% end %>
});

<% flash.clear %>
<%= yield %>
