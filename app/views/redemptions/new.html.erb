<h1>Submit a <%= @college.name %> Redemption</h1>
<%= form_for ([@college, @redemption]) do |f| %> 
<%= render 'shared/fields', :f => f %>
<div class="actions">
	<%= f.submit "Submit" %> 
</div>
<% end %>

			
			
