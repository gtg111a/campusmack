<%= render 'shared/masthead1' %>
	</table>	
		<h1><%= @college.name %> Smacks</h1>
			<table class="masthead">
			<tr>
				<td>
					Videos | Photos | News | Stats
				</td>
			</tr>
			</table>
			<% if signed_in? %>
		<a><%= link_to "Add a Smack", new_user_college_smack_path(@college) %></a>
		   <% else %>
		<a><%= link_to "Add a Smack", signin_path %></a>
			<% end %>
		<table class = "post">
		<%= render :partial => "/posts/post", :collection => @smacks %>
		</table>
		<%= will_paginate(@smacks)%>
