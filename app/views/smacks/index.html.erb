	<table class="masthead">
	<tr>
		<td>
			<%= @college.name %> |
			<%= link_to "Home", college_path(@college) %> |
			<%= link_to "Redemptions", college_redemptions_path(@college) %>
		</td>
	</tr>
	</table>	
		<h1><%= @college.name %> Smacks</h1>
			<table class="masthead">
			<tr>
				<td>
					Videos | Photos | News | Stats
				</td>
			</tr>
			</table>
		<a><%= link_to "Add a Smack", new_college_smack_path(@college) %></a>
		<table class = "post">
		<%= render @smacks %>
		</table>
		<%= will_paginate(@smacks)%>
